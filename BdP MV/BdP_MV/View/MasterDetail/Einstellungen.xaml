<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BdP_MV.View.MasterDetail.Einstellungen">
    <ContentPage.Content>
        <StackLayout VerticalOptions="StartAndExpand">
            <ActivityIndicator 
                       IsVisible="{Binding IsBusy}"
                       IsRunning="{Binding IsBusy}"
                        IsEnabled="{Binding IsBusy}"
                       VerticalOptions="CenterAndExpand"
                       HorizontalOptions="CenterAndExpand"
                       RelativeLayout.XConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=0.33}" />

            <Picker x:Name="reihenfolgePicker"
            SelectedIndexChanged="settingsChanged"

            Title="Sortierreihenfolge"
            ItemsSource="{Binding Sortierreihenfolgen}"
            SelectedItem="{Binding Sortierreihenfolge}"
            ItemDisplayBinding="{Binding Name}"/>

            <StackLayout Orientation="Horizontal">

                <Label Text="Kleingruppen laden" FontSize="Medium" />
                <Switch x:Name="kleingruppenload" IsToggled="true" Toggled="settingsChanged" />
            </StackLayout>




        </StackLayout>
    </ContentPage.Content>
</ContentPage>